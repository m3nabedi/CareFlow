"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { clinicAdminApi } from "@/lib/clinic-admin";

export default function SignInForm() {
  const router = useRouter();
  const [method, setMethod] = useState<"email" | "phone">("email");
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [phone, setPhone] = useState(""); const [code, setCode] = useState("");
  const [codeSent, setCodeSent] = useState(false); const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(""); setBusy(true); try { if (method === "email") await clinicAdminApi.login(email, password); else if (!codeSent) { await clinicAdminApi.requestPhoneCode(phone); setCodeSent(true); } else await clinicAdminApi.verifyPhoneCode(phone, code); if (method === "email" || codeSent) router.push("/admin"); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : "We could not sign you in."); } finally { setBusy(false); } }
  function chooseMethod(nextMethod: "email" | "phone") { setMethod(nextMethod); setCodeSent(false); setError(""); }
  return <div className="flex w-full flex-1 flex-col lg:w-1/2"><div className="mx-auto mb-5 w-full max-w-md pt-6 sm:pt-10"><Link href="/" className="inline-flex items-center text-sm font-medium text-gray-500 transition hover:text-gray-800 dark:text-gray-400 dark:hover:text-white">← Back to CareFlow</Link></div><div className="mx-auto flex w-full max-w-md flex-1 flex-col justify-center"><div className="mb-7"><p className="text-sm font-semibold text-brand-600 dark:text-brand-400">Clinic administration</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-gray-900 dark:text-white">Welcome back</h1><p className="mt-2 text-sm leading-6 text-gray-500">Sign in to manage forms and review your clinic’s responses.</p></div><a href={clinicAdminApi.googleUrl()} className="flex h-12 w-full items-center justify-center gap-3 rounded-lg border border-gray-300 bg-white text-sm font-semibold text-gray-700 transition hover:bg-gray-50 dark:border-gray-700 dark:bg-transparent dark:text-gray-200 dark:hover:bg-white/5"><span className="grid size-5 place-items-center rounded-full bg-white font-bold text-[#4285F4]">G</span>Continue with Google</a><div className="my-6 flex items-center gap-3 text-xs font-medium uppercase tracking-wider text-gray-400"><span className="h-px flex-1 bg-gray-200 dark:bg-gray-800" />or continue with<span className="h-px flex-1 bg-gray-200 dark:bg-gray-800" /></div><div className="mb-5 grid grid-cols-2 rounded-lg bg-gray-100 p-1 dark:bg-white/[0.05]"><button type="button" onClick={() => chooseMethod("email")} className={`rounded-md px-3 py-2 text-sm font-semibold transition ${method === "email" ? "bg-white text-gray-900 shadow-theme-xs dark:bg-gray-800 dark:text-white" : "text-gray-500"}`}>Email</button><button type="button" onClick={() => chooseMethod("phone")} className={`rounded-md px-3 py-2 text-sm font-semibold transition ${method === "phone" ? "bg-white text-gray-900 shadow-theme-xs dark:bg-gray-800 dark:text-white" : "text-gray-500"}`}>Phone</button></div><form onSubmit={submit} className="space-y-5">{method === "email" ? <><label className="block text-sm font-medium text-gray-700 dark:text-gray-300">Work email<input required value={email} onChange={(event) => setEmail(event.target.value)} type="email" autoComplete="email" placeholder="you@clinic.com" className="mt-2 h-12 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none transition focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 dark:border-gray-700 dark:bg-gray-900 dark:text-white" /></label><label className="block text-sm font-medium text-gray-700 dark:text-gray-300">Password<div className="relative mt-2"><input required value={password} onChange={(event) => setPassword(event.target.value)} type={showPassword ? "text" : "password"} autoComplete="current-password" placeholder="Enter your password" className="h-12 w-full rounded-lg border border-gray-300 bg-white px-3 pr-14 text-sm text-gray-900 outline-none transition focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 dark:border-gray-700 dark:bg-gray-900 dark:text-white" /><button type="button" onClick={() => setShowPassword((current) => !current)} className="absolute right-3 top-1/2 -translate-y-1/2 text-xs font-semibold text-brand-600 dark:text-brand-400">{showPassword ? "Hide" : "Show"}</button></div></label></> : <><label className="block text-sm font-medium text-gray-700 dark:text-gray-300">Mobile number<div className="mt-2 flex"><select aria-label="Calling code" className="h-12 rounded-l-lg border border-r-0 border-gray-300 bg-gray-50 px-2 text-sm text-gray-700 outline-none focus:border-brand-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-200"><option>+1</option><option>+44</option><option>+61</option></select><input required value={phone} onChange={(event) => setPhone(event.target.value)} type="tel" autoComplete="tel" placeholder="555 000 0000" className="h-12 min-w-0 flex-1 rounded-r-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none transition focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 dark:border-gray-700 dark:bg-gray-900 dark:text-white" /></div></label>{codeSent && <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">Verification code<input required value={code} onChange={(event) => setCode(event.target.value)} inputMode="numeric" autoComplete="one-time-code" placeholder="Enter the 6-digit code" className="mt-2 h-12 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none transition focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 dark:border-gray-700 dark:bg-gray-900 dark:text-white" /></label>}<p className="rounded-lg bg-brand-50 p-3 text-sm leading-5 text-brand-700 dark:bg-brand-500/10 dark:text-brand-200">{codeSent ? "Enter the verification code sent to your mobile number." : "We will send a one-time verification code to this number."}</p></>}<button disabled={busy} className="h-12 w-full rounded-lg bg-brand-500 text-sm font-semibold text-white shadow-theme-xs transition hover:bg-brand-600 disabled:cursor-not-allowed disabled:opacity-60">{busy ? "Please wait…" : method === "email" ? "Sign in" : codeSent ? "Verify and sign in" : "Send verification code"}</button></form>{error && <p role="alert" className="mt-4 rounded-lg border border-error-200 bg-error-50 p-3 text-sm leading-5 text-error-700">{error}</p>}<p className="mt-6 text-sm text-gray-600 dark:text-gray-400">New to CareFlow? <Link href="/signup" className="font-semibold text-brand-600 hover:text-brand-700 dark:text-brand-400">Create a clinic account</Link></p></div></div>;
}
