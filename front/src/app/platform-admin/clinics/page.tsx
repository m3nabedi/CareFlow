"use client";

import { FormEvent, useCallback, useEffect, useState } from "react";
import { platformAdminApi, type PlatformClinic } from "@/lib/platform-admin";

export default function PlatformClinicsPage() {
  const [clinics, setClinics] = useState<PlatformClinic[]>([]); const [name, setName] = useState(""); const [slug, setSlug] = useState(""); const [domain, setDomain] = useState(""); const [loading, setLoading] = useState(true); const [creating, setCreating] = useState(false); const [error, setError] = useState("");
  const loadClinics = useCallback(async () => { setLoading(true); setError(""); try { setClinics(await platformAdminApi.clinics()); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : "Could not load clinics."); } finally { setLoading(false); } }, []);
  useEffect(() => { void loadClinics(); }, [loadClinics]);
  async function createClinic(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setCreating(true); setError(""); try { await platformAdminApi.createClinic({ name, ...(slug ? { slug } : {}), ...(domain ? { public_domain: domain } : {}) }); setName(""); setSlug(""); setDomain(""); await loadClinics(); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : "Could not create clinic."); } finally { setCreating(false); } }
  return <div className="space-y-8"><section><p className="text-sm font-semibold text-brand-600">Platform owner</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Clinics</h1><p className="mt-2 text-sm text-gray-500">Create and manage the organizations using CareFlow.</p></section><section className="rounded-2xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-white/[0.03]"><h2 className="font-bold">Add clinic</h2><form onSubmit={createClinic} className="mt-5 grid gap-4 md:grid-cols-3"><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Clinic name<input required value={name} onChange={(event) => setName(event.target.value)} placeholder="Example Clinic" className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 dark:border-gray-700 dark:bg-gray-950 dark:text-white" /></label><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Slug <span className="font-normal text-gray-400">(optional)</span><input value={slug} onChange={(event) => setSlug(event.target.value)} placeholder="example-clinic" className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 dark:border-gray-700 dark:bg-gray-950 dark:text-white" /></label><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Public domain <span className="font-normal text-gray-400">(optional)</span><input value={domain} onChange={(event) => setDomain(event.target.value)} placeholder="forms.example.com" className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 dark:border-gray-700 dark:bg-gray-950 dark:text-white" /></label><div className="md:col-span-3"><button disabled={creating} className="rounded-lg bg-gray-900 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60 dark:bg-white dark:text-gray-900">{creating ? "Creating…" : "Create clinic"}</button></div></form></section>{error && <div role="alert" className="rounded-xl border border-error-200 bg-error-50 p-4 text-sm text-error-700">{error}</div>}<section className="overflow-hidden rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[0.03]"><div className="flex items-center justify-between border-b border-gray-200 p-5 dark:border-gray-800"><h2 className="font-bold">All clinics</h2><button type="button" onClick={() => void loadClinics()} className="text-sm font-semibold text-brand-600">Refresh</button></div>{loading ? <p className="p-6 text-sm text-gray-500">Loading clinics…</p> : clinics.length === 0 ? <p className="p-8 text-center text-sm text-gray-500">No clinics have been created yet.</p> : <div className="divide-y divide-gray-100 dark:divide-gray-800">{clinics.map((clinic) => <article key={clinic.id} className="flex flex-wrap items-center justify-between gap-3 p-5"><div><h3 className="font-semibold text-gray-900 dark:text-white">{clinic.name}</h3><p className="mt-1 text-sm text-gray-500">{clinic.slug ?? "No slug"}{clinic.publicDomain ? ` · ${clinic.publicDomain}` : ""}</p></div><span className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-semibold text-gray-600 dark:bg-white/10 dark:text-gray-300">{clinic.questionnairesCount ?? 0} forms</span></article>)}</div>}</section></div>;
}
