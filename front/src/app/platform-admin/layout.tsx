"use client";
import Link from "next/link";
import { useEffect, useState, type ReactNode } from "react";
import { usePathname, useRouter } from "next/navigation";
import { platformAdminTokenKey } from "@/lib/platform-admin";
export default function PlatformAdminLayout({ children }: { children: ReactNode }) { const pathname = usePathname(); const router = useRouter(); const signIn = pathname === "/platform-admin/signin"; const [allowed] = useState(() => typeof window !== "undefined" && Boolean(window.localStorage.getItem(platformAdminTokenKey))); useEffect(() => { if (!signIn && !allowed) router.replace("/platform-admin/signin"); }, [allowed, signIn, router]); if (signIn) return <>{children}</>; if (!allowed) return <main className="grid min-h-screen place-items-center bg-gray-50 text-sm text-gray-500 dark:bg-gray-950">Checking platform access…</main>; return <div className="min-h-screen bg-gray-50 text-gray-900 dark:bg-gray-950 dark:text-white"><aside className="fixed inset-y-0 hidden w-64 border-r border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-gray-900 lg:block"><Link href="/platform-admin" className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-gray-900 font-bold text-white dark:bg-white dark:text-gray-900">C</span><span className="font-bold">CareFlow Platform</span></Link><p className="mt-9 px-3 text-[11px] font-semibold uppercase tracking-wider text-gray-400">Platform</p><nav className="mt-3 space-y-1"><Link href="/platform-admin" className="block rounded-lg bg-gray-100 px-3 py-2.5 text-sm font-semibold dark:bg-white/10">Overview</Link><Link href="/platform-admin/clinics" className="block rounded-lg px-3 py-2.5 text-sm font-medium text-gray-600 hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-white/5">Clinics</Link></nav></aside><main className="mx-auto max-w-7xl p-6 lg:ml-64 lg:p-8">{children}</main></div>; }
