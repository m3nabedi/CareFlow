"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { platformAdminApi } from "@/lib/platform-admin";
export default function PlatformSignInPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(""); try { await platformAdminApi.login(email, password); router.replace("/platform-admin"); } catch (e) { setError(e instanceof Error ? e.message : "Could not sign in."); } finally { setBusy(false); } } return <main className="grid min-h-screen place-items-center bg-gray-50 p-6 dark:bg-gray-950"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-gray-200 bg-white p-7 shadow-theme-sm dark:border-gray-800 dark:bg-gray-900"><Link href="/" className="text-sm font-medium text-gray-500">← CareFlow</Link><p className="mt-7 text-sm font-semibold text-brand-600">Platform owner</p><h1 className="mt-1 text-2xl font-bold">Sign in to platform administration</h1><p className="mt-2 text-sm leading-6 text-gray-500">This area is separate from clinic administrator access.</p><label className="mt-6 block text-sm font-medium">Email<input required value={email} onChange={(e) => setEmail(e.target.value)} type="email" className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 dark:border-gray-700 dark:bg-gray-950 dark:text-white" /></label><label className="mt-5 block text-sm font-medium">Password<input required value={password} onChange={(e) => setPassword(e.target.value)} type="password" className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 dark:border-gray-700 dark:bg-gray-950 dark:text-white" /></label><button disabled={busy} className="mt-6 h-11 w-full rounded-lg bg-gray-900 text-sm font-semibold text-white disabled:opacity-60 dark:bg-white dark:text-gray-900">{busy ? "Signing in…" : "Sign in"}</button>{error && <p role="alert" className="mt-4 text-sm text-error-600">{error}</p>}</form></main>; }
