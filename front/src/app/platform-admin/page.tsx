"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { platformAdminApi, type PlatformDashboard } from "@/lib/platform-admin";
export default function PlatformAdminPage() { const [data, setData] = useState<PlatformDashboard | null>(null); const [error, setError] = useState(""); useEffect(() => { platformAdminApi.dashboard().then(setData).catch((e) => setError(e instanceof Error ? e.message : "Could not load the platform dashboard.")); }, []); return <div className="space-y-8"><section><p className="text-sm font-semibold text-brand-600">Platform owner</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Platform overview</h1><p className="mt-2 text-sm text-gray-500">Manage clinics, their access, and platform-level activity.</p></section>{error ? <div className="rounded-xl border border-error-200 bg-error-50 p-4 text-sm text-error-700">{error}</div> : <section className="grid gap-4 sm:grid-cols-3">{[["Clinics", data?.clinicsCount ?? "—"], ["Published forms", data?.questionnairesCount ?? "—"], ["Responses", data?.submissionsCount ?? "—"]].map(([label, value]) => <div key={label} className="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><p className="text-sm text-gray-500">{label}</p><p className="mt-3 text-2xl font-bold">{value}</p></div>)}</section>}<Link href="/platform-admin/clinics" className="inline-flex rounded-lg bg-gray-900 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-900">Manage clinics</Link></div>; }
