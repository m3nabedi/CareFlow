"use client";

import { FormEvent, useEffect, useState } from "react";
import { clinicAdminApi, type ClinicSettings } from "@/lib/clinic-admin";

const initialSettings: ClinicSettings = { name: "", publicDomain: "", timezone: "America/Toronto", defaultCallingCode: "+1", supportEmail: "" };

export default function ClinicSettingsPage() {
  const [settings, setSettings] = useState(initialSettings); const [status, setStatus] = useState<"loading" | "ready" | "saving" | "saved" | "error">("loading"); const [message, setMessage] = useState("");
  useEffect(() => { clinicAdminApi.clinic().then((clinic) => { setSettings(clinic); setStatus("ready"); }).catch((requestError) => { setStatus("error"); setMessage(requestError instanceof Error ? requestError.message : "We could not load clinic settings."); }); }, []);
  function update(key: keyof ClinicSettings, value: string) { setSettings((current) => ({ ...current, [key]: value })); }
  async function saveSettings(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus("saving"); setMessage(""); try { const saved = await clinicAdminApi.updateClinic(settings); setSettings(saved); setStatus("saved"); setMessage("Clinic settings have been saved."); } catch (requestError) { setStatus("error"); setMessage(requestError instanceof Error ? requestError.message : "We could not save clinic settings."); } }
  return <div className="mx-auto max-w-3xl"><div className="mb-7"><p className="text-sm font-semibold text-brand-600 dark:text-brand-400">Clinic workspace</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">Clinic settings</h1><p className="mt-2 text-sm text-gray-500">Set the details that appear across your clinic’s public forms and workspace.</p></div><form onSubmit={saveSettings} className="space-y-6"><section className="rounded-2xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-white/[0.03]"><h2 className="text-base font-bold text-gray-900 dark:text-white">Clinic profile</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Clinic name<input required disabled={status === "loading"} value={settings.name} onChange={(event) => update("name", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 disabled:opacity-60 dark:border-gray-700 dark:bg-gray-900 dark:text-white" /></label><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Default timezone<select disabled={status === "loading"} value={settings.timezone} onChange={(event) => update("timezone", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 disabled:opacity-60 dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option>America/Toronto</option><option>America/Vancouver</option><option>Europe/London</option><option>Australia/Sydney</option></select></label></div></section><section className="rounded-2xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-white/[0.03]"><h2 className="text-base font-bold text-gray-900 dark:text-white">Patient contact defaults</h2><p className="mt-1 text-sm text-gray-500">These are defaults only. Each form can use its own field settings.</p><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Default calling code<select disabled={status === "loading"} value={settings.defaultCallingCode} onChange={(event) => update("defaultCallingCode", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 disabled:opacity-60 dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option>+1</option><option>+44</option><option>+61</option></select></label><label className="text-sm font-medium text-gray-700 dark:text-gray-300">Support email<input disabled={status === "loading"} value={settings.supportEmail} onChange={(event) => update("supportEmail", event.target.value)} type="email" placeholder="clinic@example.com" className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 outline-none focus:border-brand-500 focus:ring-3 focus:ring-brand-500/10 disabled:opacity-60 dark:border-gray-700 dark:bg-gray-900 dark:text-white" /></label></div></section><div className="flex items-center justify-end gap-3"><span aria-live="polite" className={`text-sm font-medium ${status === "error" ? "text-error-600" : "text-success-600"}`}>{status === "loading" ? "Loading clinic settings…" : message}</span><button disabled={status === "loading" || status === "saving"} className="rounded-lg bg-brand-500 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-brand-600 disabled:cursor-not-allowed disabled:opacity-60">{status === "saving" ? "Saving…" : "Save changes"}</button></div></form></div>;
}
